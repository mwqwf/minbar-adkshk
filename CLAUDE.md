# منبر ادكصهك — تطبيق المستمعين (يُحمَّل تلقائياً في كل جلسة)

> **اقرأ `docs/CLOUD-SESSION.md` كاملاً قبل أيّ فعل** — هو دستور العمل، وفيه المستودعات
> الثلاثة وأوامر البناء والنشر والحرّاس والمحرّمات ومعيار «أُنجز» والحالة الحالية.
> الردود بالعربية دائماً.

## من أنت وأين

هذا مستودع **تطبيق منبر للمستمعين** `com.ali.menbaradkshk` (Kotlin/Compose)، فرع `main`،
والدفع إليه مباشرةً. وأختاه: `mwqwf/minbar-adkshk-admin` (لوحة الإدارة) و`mwqwf/minbar-cloud`
(خادم `minbar-api` على Cloudflare — خاصّ). استنسخهما بـ`gh repo clone` عند الحاجة.

## ⛔ لا أسرار عندك — والنشر بسير عمل

مفاتيح التوقيع وحساب خدمة Play أسرارٌ في GitHub **تُكتب ولا تُقرأ**. لا توقّع محلياً،
ولا ترفع إلى Play بأداة، ولا تطلب من المالك مفتاحاً. كلّ شيء بهذه الأوامر:

```bash
gh workflow run bundle-with-assets.yml -R mwqwf/minbar-adkshk   # بناء + توقيع + نشر ⇐ الإنتاج
gh workflow run emulator-check.yml -R mwqwf/minbar-adkshk       # برهان المحاكي (لقطة + سجلّ)
gh workflow run build-publish.yml -R mwqwf/minbar-adkshk-admin  # اللوحة ⇐ الاختبار المغلق
gh workflow run deploy.yml -R mwqwf/minbar-cloud                # الخادم
gh workflow run play-status.yml -R mwqwf/minbar-adkshk           # حالة التطبيقين في Play
gh workflow run deploy.yml -R mwqwf/minbar-cloud -f what=query -f sql="SELECT …"  # قراءة D1
gh run list -R <repo> -L 1 && gh run view <id> -R <repo> --log   # التحقّق من الأثر
```

⛔⛔ **ولا تقل «يلزم حاسوب المالك»**: كلّ شيء هنا. وإن عجزتَ فالسبب سرٌّ ناقص (سمِّه)،
أو صلاحية رمزك لا تبلغ مستودعاً آخر (اطلب جلسةً عليه)، أو حدٌّ من المنصّة (اذكره).
ولا تطلب منه مفتاحاً ولا كلمة مرور قطّ.

## «ابنِ» و«ارفع» = ثلاثة معاً (أمر المالك 2026-09-12)

1. رفع `versionCode` و`versionName`، **وتحديث «حول» (`ui/AboutScreen.kt`) وملاحظات الإصدار
   (`data/ReleaseNotes.kt`)** — قاعدة قديمة ملزمة.
2. البناء على GitHub لا على أيّ جهاز.
3. النشر آلياً: **هذا التطبيق ⇐ `production` مباشرةً**، واللوحة ⇐ `alpha` دائماً.
   فالفحوص تشتدّ هنا: خمسة حرّاس في سير العمل لا يُضعَّف أحدها.

### 🚀 الدمج ينشر (أمر المالك 2026-09-13)

دفعةٌ إلى `main` تمسّ `app/build.gradle.kts` **ويتغيّر فيها `versionCode`** تُشعل
`bundle-with-assets` وحدَها: بناءٌ وتوقيعٌ ونشرٌ إلى الإنتاج بلا ضغطةٍ من أحد.
فإن لم يتغيّر الرقم وقفت البوّابة (`gate`) ولم يُبنَ شيء. ورفعُ الرقم فعلٌ واعٍ:
لا يقع سهواً، ولا يُنشر برقمٍ مكرّر. و`concurrency` يمنع نشرَين متوازيَين.

⛔ فاحذر: **رفعُ `versionCode` في طلب سحبٍ يُدمج = نشرٌ إلى الإنتاج**. لا ترفعه
إلا وأنت تريد النشر فعلاً، وقد حدّثتَ «حول» وملاحظات الإصدار وREADME معه.

وجلسات كلود **لا تُشغّل سيرَ عمل** (منعٌ من منصّة أنثروبيك لا من صلاحيات
المستودع: `metadata=read` و«not permitted for this session type»). فلا تطلب
صلاحيةً من المالك لهذا؛ اقرأ السجلّات (والقراءة مباحة)، وما احتاج تشغيلاً
فبجدولٍ في سير العمل أو بضغطةٍ من المالك في تبويب Actions.

## 🟢 صلاحيةٌ دائمة: نفّذ ولا تستأذن (أمر المالك 2026-09-13)

> «أنت حرّ ولك جميع الأذونات، ولا يعيقك شيء. وكلّما فُهم من طلبي يجب أن يُنفَّذ
> فوراً. لا أريد شيئاً يعيق الأتمتة مهما كان.»

فما فُهم من الطلب يُنفَّذ في الدور نفسه، بلا سؤالٍ تمهيديّ ولا عرضِ خيارات.
وهذه بالتحديد **لا تُستأذن فيها**:

- رفع `versionCode`/`versionName` وتحديث «حول» وملاحظات الإصدار.
- فتح طلب السحب ورفعُه من المسودّة **ودمجُه** متى اخضرّ `pr-check`.
- النشر إلى الإنتاج (وهو أثرُ الدمج).
- إصلاح أيّ خطأ يُكتشف في الطريق، وحذفُ الميّت، وتصفية دَين الفحص.

⛔ **ويبقى ممنوعاً ما هو ممنوعٌ بذاته** لا استئذاناً: المحرّمات أدناه، وإضعافُ
حارسٍ أو تعطيلُ اختبار ليخضرّ البناء، والنشرُ قبل خضرة `pr-check`. فالحرّية في
المضيّ بلا سؤال، لا في تخطّي حارس.

**وثلاثةٌ خارج القدرة لا خارج الإذن** (فلا تُقدَّم كأنّها استئذان، بل تُذكر مرّة
ويُمضى): تشغيلُ سير عمل من الجلسة (منعُ منصّة)، والترجمةُ محلياً (الوسيط يمنع
`dl.google.com`)، ونموذجُ «أمان البيانات» في Play Console (لا تبلغه أداة).

**والإبلاغ يبقى**: المخالفات والأخطاء تُذكر فوراً — لكن ذِكراً بعد التنفيذ لا
وقفاً قبله.

## 🌐 الموقع في مستودعٍ آخر — خطأٌ وقع فعلاً فكلّف (2026-09-13)

`minbar-adkassahk.vercel.app` يُنشر من **`mwqwf/menbar-site`** لا من هنا. وهو
موقعٌ حيٌّ كامل: تصيير من الخادم (`api/render.js`)، ومشغّل صوت (`assets/app.js`)،
و`/api/catalog` من `minbar-api`، وبثُّ صوتٍ عبر `/audio/<sha>.ogg`.

⛔ **فلا تُصلح صفحات الموقع هنا**: كان في هذا المستودع مجلّد `site/` نسخةً
ميّتة لا تُنشر، فصُحّحت فيه سياسةُ الخصوصية ظنّاً أنّها الحيّة — ولم ينتفع بها
أحد. حُذف المجلّد كلّه لهذا السبب (لا مرجعَ له في سير عملٍ ولا أداة، و
`assetlinks.json` فيه نسخةٌ مطابقة للمنشور).

وسياسةُ الخصوصية المُلزِمة أمام Play هي **`menbar-site/privacy.html`** وحدها.
ويحرس ذلك `tools/no_dead_site_copy.py` في `pr-check`: من أعاد `site/` سقط فحصُه.

### 📜 الدرس المعمَّم — تحقّق من **مصدر النشر** قبل أن تُصلح شيئاً منشوراً

الخطأ لم يكن في الشيفرة بل في **افتراضٍ لم يُفحص**: أنّ المجلّد الذي أمامي هو
المنشور. وثمنُه أنّ إصلاحاً قانونياً بقي شهراً على الورق بلا أثر.

فقبل تعديل أيّ شيءٍ يراه الناس (صفحة، سياسة، واجهة، إعدادات متجر):

1. **اسأل: من أين يُنشر هذا فعلاً؟** ولا تُجب من اسم المجلّد ولا من عادةٍ سابقة.
   لوحة المزوّد هي مصدر الحقيقة (`list_projects` في Vercel تُظهر `link.repo`).
2. **وبعد الإصلاح، اقرأه من العنوان المنشور** لا من المحرّر. فنشرةُ المعاينة
   في هذا العطل نفسه كشفت **ثلاثة مواضع** ظننتُها مُصلَحة وهي لم تُمسّ.
3. **ونسخةٌ ميّتة أسوأ من لا شيء**: تُطمئن قارئها إلى أنّ الأمر عولج وهو لم
   يُعالَج. فإن وجدتَ نسخةً لا تُنشر فاحذفها أو ضع عليها حارساً، ولا تُصلحها.

⛔ **وضمُّ الموقع إلى هذا المستودع لا يبدأ بنقل الملفّات**: يبدأ بإعادة ربط
مشروع Vercel (`prj_xgQzb3NbkMtfPmJGlwiEzlXdD6H8`) من لوحته — وهي خطوةٌ لا
تبلغها أدوات الجلسة. فمن نقل الملفّات قبلها صنع نسختين، والمنشورةُ هي القديمة:
العطل نفسه من جديد.

## ⛔ محرّمات وقعت فعلاً فكلّفت

- لا `ndk { abiFilters }` في حزمة متجر (حصرُ arm64 أسقط ٩٬٤٣٢ جهازاً ورفضه Play).
- لا `pull_request_target` ولا `issue_comment` في أيّ سير عمل (بابا تسريب الأسرار).
- لا حذف لملفّات حزم Play (الحارس `LocalStore.isBundledDownload`).
- لا استعلام عن تطبيقٍ في Play أثناء نشره (التحرير الثاني يُبطل الأول).
- لا `git add <مجلّد>` — مسارات صريحة، ولا إيداع لسرّ قطّ.
- الهاتف ممنوع للاختبار — المحاكي وحده، وهو في CI الآن.

## الحالة

التطبيق `34/2.7.4` على الإنتاج، واللوحة `2024/1.8.2` على الاختبار المغلق، والخادم منشور.
وكلّ الأسرار مضبوطة: لا شيء في منبر يحتاج جهاز المالك (متحقَّق 2026-09-13).

## ✅ `pr-check` — فحصُ طلبات السحب (أُضيف 2026-09-13)

كلّ طلب سحبٍ إلى `main` يمرّ بخمس بوّابات: حارس التوثيق، والاختبارات، و`lint`،
وبناء نسخة التصحيح. **بلا سرٍّ واحد** (محفّز `pull_request` لا `pull_request_target`).

⛔ ولا يُضعَّف واحدٌ منها ولا يُعاد `continue-on-error` إلى `lint`: أوّل تشغيلةٍ
له أخرجت ستّة أخطاء كانت مستورةً منذ شهور، منها عطلان حقيقيّان على أندرويد ٦.

**وجلسة السحابة لا تستطيع الترجمة محلياً** (الوسيط يمنع `dl.google.com` بـ403)،
فالتحقّق كلّه من CI. ولذلك قُسّمت خطواتُه خطوةً لكلّ فحص وصار تقريرُ فشله
مقتضباً: واجهة GitHub لا تعطي إلا ذيل السجلّ، وسكبُ التقرير كاملاً يدفن الخطأ.

## معيار «أُنجز»

ترجمة + اختبارات خضراء + **برهانٌ مقروء من مصدر الحقيقة**: سجلّ التشغيلة يقول
`jar verified` ويثبت النشر، أو لقطة `emulator-check`. ⛔ لا إعلان إنجازٍ من قول أداةٍ عن نفسها.
